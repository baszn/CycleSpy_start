The 'tests' folder will contain the actual finished test, where the assembly is placed inside the template.
Note that base_address_low and base_address_high aren't replaced yet as these are depended on the microcontroller
the test is run on.